<p id="notice"><%= notice %></p>

<p>
  <strong>Room no:</strong>
  <%= @room.room_no %>
</p>

<p>
  <strong>Building:</strong>
  <%= @room.building %>
</p>

<p>
  <strong>Size:</strong>
  <%= @room.size %>
</p>

<p>
  <strong>Status:</strong>
  <%= @room.status %>
</p>

<%= form_tag(email_notifier_path, method: :get) do %>
    <strong>Emails:</strong>
    <%= text_field_tag :emails %>
   <!-- <%=email_field_tag 'emails[]','' ,:placeholder=>"Type an email address",:pattern=>"^([\w+-.%]+@[\w-.]+\.[A-Za-z]{2,4},*[\W]*)+$",:multiple => true%>-->
    <%= submit_tag 'Send Email'%>
<% end %>

<%= link_to 'Edit', edit_room_path(@room) %> |
<%= link_to 'Back', root_path %>
