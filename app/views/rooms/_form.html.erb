<%= form_for(@room) do |f| %>
  <% if @room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
      <% @room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :room_no %><br>
    <%= f.number_field :room_no, :required => true%>
  </div>
  <div class="field">
    <%= f.label :building %><br>
    <%= f.select :building, ['D.H.Hill', 'James.B.Hunt'] %>
  </div>
  <div class="field">
    <%= f.label :size %><br>
    <%= f.select :size, [4, 6, 8] %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.select :status, ['Booked', 'Available'] %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
